using UnityEngine;

public class musicDisabledChecker : MonoBehaviour {
	//AudioSource source;
	// Use this for initialization
	//void OnEnable () {
	//	source = GetComponent<AudioSource>();
	//}
	
	// Update is called once per frame
	/*void Update ()
	{
		if(source.isPlaying)
		Debug.Log("Playing");
		else if(!source.isPlaying)
		Debug.Log("Stopped");
	}*/
}
